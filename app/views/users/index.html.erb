<h2>Admin Section</h2>

<% @users.each do |user| %>
  <div class="panel-non-link">
    <%= "Username: #{user.username}  | "%>

    <%= "Email: #{ user.email }"%>

    <%= link_to "Edit", edit_user_path(user), id: user.id, class: 'btn-and-link' %>
  </div>


  <br />
<% end %>
